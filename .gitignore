__pycache__/
*.pyc
.venv/
.env
.DS_Store
.streamlit/secrets.toml

# Raw v2 Socrata pulls. Already-tracked files keep being tracked;
# this ignores future re-fetches from scripts/10 so they don't show
# up as diffs every time the pipeline is re-run.
data/raw_v2/
